# Statistical appendix

The primary unit is held-out anchor (n=78). Three model seeds quantify optimization variation descriptively. No p-values are used: the comparisons are paired deterministic decompositions on a fixed audit set. Correlations are reported as Pearson and Spearman when both variables have nonzero variance; otherwise they are N/A.
