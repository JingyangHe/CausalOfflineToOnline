# Phase 8G-Q Exact Joint Shared-World Gate

Positive control: `matched_opposite_M2`. Both sources have public action marginal `(0.45, 0.10, 0.45)`; their hidden-response selection tables are exact reversals.

The LP uses public finite reward supports and public source-conditioned joint mass only. The do oracle is isolated to final evaluation. See the CSV tables for the seven gate questions.
